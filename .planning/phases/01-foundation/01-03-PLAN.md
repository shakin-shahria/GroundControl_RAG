---
phase: 01-foundation
plan: 03
type: execute
wave: 3
depends_on:
  - 01-02
files_modified:
  - backend/app/models/user.py
  - backend/app/models/document.py
  - backend/app/models/request.py
  - backend/app/models/evaluation.py
  - backend/app/models/__init__.py
  - backend/alembic/env.py
  - backend/alembic/versions/0001_initial_schema.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02

must_haves:
  truths:
    - "All five tables exist in PostgreSQL after Alembic migration runs: users, documents, document_chunks, ai_requests, evaluations"
    - "Alembic env.py uses the async pattern (asyncio.run + async_engine_from_config)"
    - "All model files are imported in alembic/env.py before target_metadata assignment so autogenerate finds all tables"
    - "Each model file contains both the SQLAlchemy ORM class AND collocated Pydantic schemas (per D-04)"
    - "User model has id, email, hashed_password, is_admin, created_at columns"
  artifacts:
    - path: "backend/app/models/user.py"
      provides: "User ORM model + UserCreate/UserResponse Pydantic schemas"
      contains: "class User(Base)"
    - path: "backend/app/models/document.py"
      provides: "Document + DocumentChunk ORM models"
      contains: "class Document(Base)"
    - path: "backend/app/models/request.py"
      provides: "AIRequest ORM model"
      contains: "class AIRequest(Base)"
    - path: "backend/app/models/evaluation.py"
      provides: "Evaluation ORM model"
      contains: "class Evaluation(Base)"
    - path: "backend/alembic/versions/0001_initial_schema.py"
      provides: "Initial migration creating all five tables"
      contains: "op.create_table"
  key_links:
    - from: "backend/alembic/env.py"
      to: "backend/app/models/__init__.py"
      via: "from app.models import user, document, request, evaluation"
      pattern: "target_metadata = Base.metadata"
    - from: "backend/alembic/versions/0001_initial_schema.py"
      to: "PostgreSQL tables"
      via: "alembic upgrade head in docker-compose command"
      pattern: "op.create_table.*users"
---

<objective>
Define all five SQLAlchemy ORM models (users, documents, document_chunks, ai_requests, evaluations) with collocated Pydantic schemas per D-04. Rewrite Alembic env.py for async mode. Generate the initial migration file that creates all five tables in PostgreSQL on first boot.

Purpose: FOUND-01 — the PostgreSQL schema. All future phases depend on these tables existing. The models also define the column contracts that auth, ingestion, query, and evaluation phases build on.
Output: Four model files, updated models/__init__.py, corrected alembic/env.py, and initial migration file 0001_initial_schema.py.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-02-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts established by Plan 02 that this plan imports -->

From backend/app/core/database.py:
  Base: DeclarativeBase subclass — all ORM models inherit from this
  async_session_maker: for use in user_service.py (Plan 04)
  get_db: FastAPI dependency yielding AsyncSession

From backend/app/core/config.py:
  settings.database_url: str — used in alembic/env.py to connect for migrations

<!-- Contracts this plan creates for Plan 04 to import -->

From backend/app/models/user.py (to be created):
  class User(Base): __tablename__ = "users"
    id: Mapped[int], email: Mapped[str], hashed_password: Mapped[str],
    is_admin: Mapped[bool], created_at: Mapped[datetime]
  class UserCreate(BaseModel): email: EmailStr, password: str (max 72 bytes validated)
  class UserResponse(BaseModel): id, email, is_admin, created_at; model_config from_attributes=True
  class UserLogin(BaseModel): email: EmailStr, password: str
  class TokenResponse(BaseModel): access_token: str, token_type: str = "bearer"
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create all five ORM model files with collocated Pydantic schemas</name>
  <files>
    backend/app/models/user.py,
    backend/app/models/document.py,
    backend/app/models/request.py,
    backend/app/models/evaluation.py,
    backend/app/models/__init__.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Code Examples section: complete users table ORM model)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-04: Pydantic schemas collocated with ORM models)
    - .planning/REQUIREMENTS.md (FOUND-01: five table names and their purpose)
    - backend/app/core/database.py (Base class to import)
  </read_first>
  <behavior>
    - UserCreate password field must have @field_validator that raises ValueError if len(v.encode("utf-8")) > 72
    - UserResponse.model_validate(user_orm_instance) must succeed (model_config from_attributes=True)
    - Document model has status field defaulting to "pending" (for ingestion state machine in Phase 2)
    - AIRequest model has nullable foreign key to users.id
    - Evaluation model has nullable foreign key to ai_requests.id
  </behavior>
  <action>
    Create backend/app/models/user.py:
    SQLAlchemy User(Base): __tablename__ = "users". Columns: id (Integer, primary_key, index), email (String, unique, index, nullable=False), hashed_password (String, nullable=False), is_admin (Boolean, default=False, nullable=False), created_at (DateTime(timezone=True), default=lambda: datetime.now(timezone.utc), nullable=False). Import Mapped, mapped_column from sqlalchemy.orm; Boolean, DateTime, Integer, String from sqlalchemy.
    Pydantic schemas (same file, below the ORM class): UserCreate(BaseModel) with email: EmailStr and password: str plus @field_validator("password") that checks len(v.encode("utf-8")) > 72 raises ValueError. UserResponse(BaseModel) with id: int, email: str, is_admin: bool, created_at: datetime, model_config = {"from_attributes": True}. UserLogin(BaseModel) with email: EmailStr and password: str. TokenResponse(BaseModel) with access_token: str and token_type: str = "bearer".

    Create backend/app/models/document.py:
    SQLAlchemy Document(Base): __tablename__ = "documents". Columns: id (Integer, primary_key, index), filename (String, nullable=False), domain (String, nullable=True), status (String, default="pending", nullable=False), page_count (Integer, nullable=True), created_at (DateTime timezone=True). SQLAlchemy DocumentChunk(Base): __tablename__ = "document_chunks". Columns: id (Integer, primary_key), document_id (Integer, ForeignKey("documents.id"), index, nullable=False), chunk_index (Integer, nullable=False), content (Text, nullable=False), page_number (Integer, nullable=True), section_header (String, nullable=True), token_count (Integer, nullable=True), chroma_id (String, nullable=True). Import Text from sqlalchemy, ForeignKey from sqlalchemy. Collocated stub Pydantic schemas: DocumentResponse(BaseModel) with id, filename, domain, status, created_at; model_config from_attributes=True.

    Create backend/app/models/request.py:
    SQLAlchemy AIRequest(Base): __tablename__ = "ai_requests". Columns: id (Integer, primary_key, index), user_id (Integer, ForeignKey("users.id"), nullable=True, index), query_text (Text, nullable=False), response_text (Text, nullable=True), fallback_triggered (Boolean, default=False), embed_ms (Integer, nullable=True), retrieve_ms (Integer, nullable=True), rerank_ms (Integer, nullable=True), generate_ms (Integer, nullable=True), validate_ms (Integer, nullable=True), total_ms (Integer, nullable=True), model_version (String, nullable=True), token_usage (Integer, nullable=True), error_type (String, nullable=True), created_at (DateTime timezone=True). Collocated stub Pydantic: AIRequestResponse(BaseModel) with id, user_id, query_text, fallback_triggered, created_at; model_config from_attributes=True.

    Create backend/app/models/evaluation.py:
    SQLAlchemy Evaluation(Base): __tablename__ = "evaluations". Columns: id (Integer, primary_key), request_id (Integer, ForeignKey("ai_requests.id"), nullable=False, index), faithfulness_score (Float, nullable=True), relevance_score (Float, nullable=True), correctness_score (Float, nullable=True), judge_model (String, nullable=True), evaluated_at (DateTime timezone=True, default=lambda: datetime.now(timezone.utc)). Import Float from sqlalchemy. Collocated stub Pydantic: EvaluationResponse(BaseModel) with id, request_id, faithfulness_score, relevance_score, correctness_score; model_config from_attributes=True.

    Update backend/app/models/__init__.py to import all four model modules so they register with Base.metadata:
    from app.models import user, document, request, evaluation  # noqa: F401
    from app.core.database import Base  # re-export for convenience
    __all__ = ["Base", "user", "document", "request", "evaluation"]
  </action>
  <verify>
    <automated>grep -n "class User\|class Document\|class DocumentChunk\|class AIRequest\|class Evaluation" backend/app/models/user.py backend/app/models/document.py backend/app/models/request.py backend/app/models/evaluation.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/models/user.py contains "class User(Base)" with __tablename__ = "users"
    - backend/app/models/user.py contains "class UserCreate(BaseModel)" with @field_validator for password 72-byte limit
    - backend/app/models/user.py contains UserResponse with model_config = {"from_attributes": True}
    - backend/app/models/document.py contains both "class Document(Base)" and "class DocumentChunk(Base)"
    - backend/app/models/request.py contains "class AIRequest(Base)" with embed_ms, retrieve_ms, rerank_ms, generate_ms, validate_ms columns
    - backend/app/models/evaluation.py contains "class Evaluation(Base)" with faithfulness_score, relevance_score, correctness_score
    - backend/app/models/__init__.py imports all four model modules (user, document, request, evaluation)
  </acceptance_criteria>
  <done>All five ORM tables defined with correct columns. Collocated Pydantic schemas per D-04. models/__init__.py imports all modules so Alembic autogenerate finds all tables.</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite alembic/env.py for async mode and generate initial migration</name>
  <files>
    backend/alembic/env.py,
    backend/alembic/versions/0001_initial_schema.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 4: Alembic Async env.py; Pitfall 2: missing model imports; Pitfall 6: asyncio.run conflicts; Code Examples: Alembic initial migration note)
    - backend/alembic/env.py (the generated async template to update — read its current state)
    - backend/app/models/__init__.py (confirm import structure)
    - backend/alembic.ini (confirm script_location and sqlalchemy.url handling)
  </read_first>
  <action>
    Rewrite backend/alembic/env.py to use the async pattern. The generated async template from `alembic init -t async` is mostly correct — make these specific changes:
    1. Add model imports BEFORE target_metadata assignment: from app.models import user, document, request, evaluation  # noqa: F401 AND from app.core.database import Base
    2. Set target_metadata = Base.metadata (not None)
    3. Override the sqlalchemy.url by setting it from Pydantic Settings: In run_migrations_online(), before building the connectable, add: config.set_main_option("sqlalchemy.url", settings.database_url) where settings is imported from app.core.config. This means the alembic.ini URL placeholder is unused.
    4. Ensure run_migrations_online() uses asyncio.run(run_async_migrations()) pattern (the async template already does this).
    5. The async template uses async_engine_from_config + NullPool — preserve this.

    Generate the initial migration file. Because this is inside Docker and the DB may not be running locally, write the migration file MANUALLY rather than using --autogenerate. Create backend/alembic/versions/0001_initial_schema.py with:
    - revision = "0001" (or a valid UUID-style revision string)
    - down_revision = None (first migration)
    - upgrade() function: uses op.create_table() to create all five tables: users, documents, document_chunks, ai_requests, evaluations with the exact columns defined in the model files
    - downgrade() function: drops all five tables in reverse dependency order (evaluations, ai_requests, document_chunks, documents, users)

    Column specs for migration (must match model definitions exactly):
    users: id (Integer, primary_key), email (String, unique, nullable=False), hashed_password (String, nullable=False), is_admin (Boolean, server_default="false", nullable=False), created_at (DateTime timezone=True, server_default=func.now())
    documents: id, filename (String), domain (String, nullable=True), status (String, server_default="pending"), page_count (Integer, nullable=True), created_at (DateTime timezone=True, server_default=func.now())
    document_chunks: id, document_id (Integer, ForeignKey documents.id), chunk_index (Integer), content (Text), page_number (Integer nullable), section_header (String nullable), token_count (Integer nullable), chroma_id (String nullable)
    ai_requests: id, user_id (Integer, ForeignKey users.id nullable), query_text (Text), response_text (Text nullable), fallback_triggered (Boolean server_default false), embed_ms through validate_ms (Integer nullable each), total_ms (Integer nullable), model_version (String nullable), token_usage (Integer nullable), error_type (String nullable), created_at (DateTime timezone=True server_default func.now())
    evaluations: id, request_id (Integer, ForeignKey ai_requests.id), faithfulness_score (Float nullable), relevance_score (Float nullable), correctness_score (Float nullable), judge_model (String nullable), evaluated_at (DateTime timezone=True server_default func.now())

    Add appropriate indexes: op.create_index on users.email, document_chunks.document_id, ai_requests.user_id, evaluations.request_id.
  </action>
  <verify>
    <automated>grep -n "target_metadata\|from app.models\|asyncio.run\|run_async_migrations" backend/alembic/env.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/alembic/env.py contains "from app.models import user, document, request, evaluation" BEFORE the target_metadata line
    - backend/alembic/env.py contains "target_metadata = Base.metadata" (not "target_metadata = None")
    - backend/alembic/env.py contains "asyncio.run" (confirms async migration pattern)
    - backend/alembic/env.py reads database_url from settings (config.set_main_option or equivalent — not hardcoded)
    - backend/alembic/versions/0001_initial_schema.py exists
    - backend/alembic/versions/0001_initial_schema.py contains "op.create_table('users'" and "op.create_table('evaluations'"
    - downgrade() in 0001_initial_schema.py drops tables in correct order (evaluations before ai_requests, document_chunks before documents)
  </acceptance_criteria>
  <done>Alembic env.py wired for async mode. All five table models are registered in metadata before env.py reads it. Initial migration creates all five tables with correct columns and FK constraints. Running alembic upgrade head creates the complete schema.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Alembic → PostgreSQL | Migration connection uses same asyncpg DSN from settings; runs before FastAPI starts |
| FastAPI → PostgreSQL | All queries via SQLAlchemy ORM (parameterized) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-09 | Tampering | SQL injection via ORM | mitigate | All DB access via SQLAlchemy ORM mapped queries; no raw text() with f-strings |
| T-01-10 | Elevation of Privilege | is_admin flag default | mitigate | is_admin defaults to False in ORM model; only admin seeding via env vars sets True; no user-facing field in UserCreate schema |
| T-01-11 | Tampering | Migration file integrity | accept | Migration file is committed to version control; changes are visible in git diff; no runtime generation |
| T-01-12 | Elevation of Privilege | 72-byte password bypass | mitigate | @field_validator in UserCreate validates byte length before request reaches service layer; bcrypt 5.x ValueError is second defense |
</threat_model>

<verification>
After both tasks complete:
- All five model classes defined (User, Document, DocumentChunk, AIRequest, Evaluation)
- alembic/env.py imports models before target_metadata
- alembic/env.py uses asyncio.run(run_async_migrations())
- 0001_initial_schema.py has both upgrade() and downgrade()
- If DB is accessible: alembic upgrade head completes without error
</verification>

<success_criteria>
- FOUND-01: Five tables defined in ORM models, schema creatable via Alembic
- Pydantic schemas collocated per D-04 (all in same file as ORM model)
- UserCreate validates 72-byte password limit
- Alembic env.py is async-safe (no syncio.run conflict with FastAPI event loop)
- Initial migration file creates tables in dependency order with FK constraints
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-03-SUMMARY.md` when done
</output>
