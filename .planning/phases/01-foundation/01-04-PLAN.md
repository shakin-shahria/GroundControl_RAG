---
phase: 01-foundation
plan: 04
type: execute
wave: 4
depends_on:
  - 01-03
files_modified:
  - backend/app/api/deps.py
  - backend/app/api/router.py
  - backend/app/api/v1/auth.py
  - backend/app/api/v1/health.py
  - backend/app/api/v1/users.py
  - backend/app/services/user_service.py
  - backend/app/main.py
autonomous: true
requirements:
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "POST /api/v1/auth/register creates a user with bcrypt-hashed password and returns UserResponse"
    - "POST /api/v1/auth/login returns a JWT access token for valid credentials"
    - "GET /api/v1/users/me returns 200 with user info when a valid JWT is provided in Authorization header"
    - "GET /api/v1/users/me returns 401 when no token is provided"
    - "GET /api/v1/users/me returns 401 when an expired or invalid token is provided"
    - "GET /health returns 200 with status ok (public, no auth required)"
    - "Admin is seeded on startup from ADMIN_EMAIL and ADMIN_PASSWORD env vars if no admin exists"
    - "Duplicate email registration returns 409 Conflict"
  artifacts:
    - path: "backend/app/api/deps.py"
      provides: "get_current_user FastAPI dependency"
      contains: "get_current_user"
    - path: "backend/app/api/v1/auth.py"
      provides: "POST /register and POST /login"
      contains: "router.post.*register"
    - path: "backend/app/api/v1/users.py"
      provides: "GET /users/me protected route"
      contains: "Depends(get_current_user)"
    - path: "backend/app/services/user_service.py"
      provides: "register, authenticate, get_user_by_id, seed_admin_if_absent"
      contains: "seed_admin_if_absent"
  key_links:
    - from: "backend/app/api/v1/users.py GET /users/me"
      to: "backend/app/api/deps.py get_current_user"
      via: "Depends(get_current_user)"
      pattern: "Depends.*get_current_user"
    - from: "backend/app/api/deps.py"
      to: "backend/app/core/security.py"
      via: "decode_access_token(token)"
      pattern: "decode_access_token"
    - from: "backend/app/main.py lifespan"
      to: "backend/app/services/user_service.py"
      via: "await seed_admin_if_absent()"
      pattern: "seed_admin_if_absent"
    - from: "backend/app/api/router.py"
      to: "app.main.py"
      via: "app.include_router(router, prefix='/api/v1')"
      pattern: "include_router"
---

<objective>
Build the complete auth flow: user_service.py (business logic), deps.py (JWT dependency), auth routes (register + login), users route (protected /me), health route (/health), router aggregator, and final main.py wiring. After this plan, the system is fully functional end-to-end.

Purpose: FOUND-03 — JWT auth. FOUND-04 — health endpoint. After this plan the Docker Compose stack can boot and a user can register, login, and access a protected route.
Output: user_service.py, deps.py, auth.py, health.py, users.py, router.py, and updated main.py with router mounted.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-03-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- Contracts from prior plans that this plan imports -->

From backend/app/core/security.py:
  hash_password(password: str) -> str
  verify_password(plain: str, hashed: str) -> bool
  create_access_token(user_id: int) -> str
  decode_access_token(token: str) -> dict  # raises HTTPException 401 on failure

From backend/app/core/database.py:
  async_session_maker: async_sessionmaker[AsyncSession]
  get_db: async def get_db() -> AsyncGenerator[AsyncSession, None]
  Base: DeclarativeBase

From backend/app/core/config.py:
  settings.admin_email: str
  settings.admin_password: str
  settings.jwt_algorithm: str

From backend/app/models/user.py:
  class User(Base): id, email, hashed_password, is_admin, created_at
  class UserCreate(BaseModel): email: EmailStr, password: str (72-byte validated)
  class UserResponse(BaseModel): id, email, is_admin, created_at; from_attributes=True
  class UserLogin(BaseModel): email: EmailStr, password: str
  class TokenResponse(BaseModel): access_token: str, token_type: str

<!-- Contracts this plan creates for Plan 05 tests to consume -->

POST /api/v1/auth/register: body UserCreate → 201 UserResponse | 409 if email taken
POST /api/v1/auth/login: body UserLogin (or OAuth2 form) → 200 TokenResponse | 401 if invalid
GET /api/v1/users/me: Authorization: Bearer <token> → 200 UserResponse | 401 if no/invalid token
GET /health: no auth → 200 {"status": "ok", "version": "1.0.0"}
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create user_service.py and deps.py</name>
  <files>
    backend/app/services/user_service.py,
    backend/app/api/deps.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 7: JWT dependency; Pattern 10: Admin bootstrap; Code Examples: FastAPI dependency injection)
    - .planning/phases/01-foundation/01-CONTEXT.md (Claude's Discretion: admin bootstrap approach)
    - backend/app/core/security.py (hash_password, verify_password, create_access_token, decode_access_token signatures)
    - backend/app/models/user.py (User ORM columns; UserCreate, UserResponse schemas)
    - backend/app/core/database.py (async_session_maker, get_db)
  </read_first>
  <behavior>
    - register_user(db, user_create) returns User ORM instance; raises HTTPException 409 if email already exists
    - authenticate_user(db, email, password) returns User if credentials valid; returns None if not found; returns None if password wrong (never reveals which failed — treat both as invalid credentials)
    - get_user_by_id(db, user_id) returns User or None
    - seed_admin_if_absent() is idempotent — does nothing if any user with is_admin=True already exists
    - get_current_user(token, db) returns User ORM instance; raises 401 if token invalid, expired, or user_id not found in DB
    - oauth2_scheme tokenUrl points to /api/v1/auth/login
  </behavior>
  <action>
    Create backend/app/services/user_service.py:
    Imports: AsyncSession from sqlalchemy.ext.asyncio, select from sqlalchemy, HTTPException + status from fastapi. Import User from app.models.user. Import hash_password, verify_password from app.core.security. Import settings from app.core.config. Import async_session_maker from app.core.database.

    async def register_user(db: AsyncSession, user_create) -> User:
      Check if email already taken: result = await db.execute(select(User).where(User.email == user_create.email)). If result.scalar_one_or_none() is not None: raise HTTPException(status_code=409, detail="Email already registered").
      Create user: user = User(email=user_create.email, hashed_password=hash_password(user_create.password), is_admin=False).
      db.add(user); await db.commit(); await db.refresh(user); return user.

    async def authenticate_user(db: AsyncSession, email: str, password: str) -> User | None:
      result = await db.execute(select(User).where(User.email == email)).
      user = result.scalar_one_or_none().
      if user is None: return None.
      if not verify_password(password, user.hashed_password): return None.
      return user.

    async def get_user_by_id(db: AsyncSession, user_id: int) -> User | None:
      result = await db.execute(select(User).where(User.id == user_id)).
      return result.scalar_one_or_none().

    async def seed_admin_if_absent() -> None:
      async with async_session_maker() as session:
        result = await session.execute(select(User).where(User.is_admin == True).limit(1))
        if result.scalar_one_or_none() is not None: return
        admin = User(email=settings.admin_email, hashed_password=hash_password(settings.admin_password), is_admin=True)
        session.add(admin); await session.commit()

    Create backend/app/api/deps.py:
    Imports: Annotated from typing, Depends + HTTPException + status from fastapi, OAuth2PasswordBearer from fastapi.security, AsyncSession from sqlalchemy.ext.asyncio. Import get_db from app.core.database. Import decode_access_token from app.core.security. Import get_user_by_id from app.services.user_service. Import User from app.models.user.

    oauth2_scheme = OAuth2PasswordBearer(tokenUrl="/api/v1/auth/login").

    async def get_current_user(token: Annotated[str, Depends(oauth2_scheme)], db: Annotated[AsyncSession, Depends(get_db)]) -> User:
      payload = decode_access_token(token)  # raises 401 on failure
      user_id_str = payload.get("sub")
      if user_id_str is None: raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid token payload")
      try: user_id = int(user_id_str)
      except ValueError: raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="Invalid user ID in token")
      user = await get_user_by_id(db, user_id)
      if user is None: raise HTTPException(status_code=status.HTTP_401_UNAUTHORIZED, detail="User not found")
      return user
  </action>
  <verify>
    <automated>grep -n "seed_admin_if_absent\|authenticate_user\|register_user\|get_current_user\|oauth2_scheme" backend/app/services/user_service.py backend/app/api/deps.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/services/user_service.py contains "async def register_user", "async def authenticate_user", "async def get_user_by_id", "async def seed_admin_if_absent"
    - register_user raises HTTPException with status_code=409 when email exists (not 400 or 500)
    - authenticate_user returns None on invalid credentials (does not raise; the caller raises 401)
    - seed_admin_if_absent uses async_session_maker (not get_db — it is called outside request context)
    - backend/app/api/deps.py contains oauth2_scheme with tokenUrl="/api/v1/auth/login"
    - get_current_user raises HTTPException 401 if user_id not found (not 404)
  </acceptance_criteria>
  <done>User service provides all CRUD + auth operations. get_current_user dependency enforces JWT on any route that declares it. Admin seeding is idempotent and reads from env vars.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create API route files, router aggregator, and finalize main.py</name>
  <files>
    backend/app/api/v1/auth.py,
    backend/app/api/v1/health.py,
    backend/app/api/v1/users.py,
    backend/app/api/router.py,
    backend/app/main.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Pattern 3: FastAPI lifespan; Pattern 7: JWT dependency; Code Examples: FastAPI dependency injection for DB session)
    - .planning/phases/01-foundation/01-CONTEXT.md (D-03: routes versioned under /api/v1/)
    - backend/app/api/deps.py (get_current_user signature)
    - backend/app/services/user_service.py (register_user, authenticate_user signatures)
    - backend/app/models/user.py (UserCreate, UserLogin, UserResponse, TokenResponse)
    - backend/app/main.py (current skeleton — read before overwriting)
  </read_first>
  <behavior>
    - POST /api/v1/auth/register with valid UserCreate body returns 201 with UserResponse JSON
    - POST /api/v1/auth/register with duplicate email returns 409
    - POST /api/v1/auth/register with password > 72 bytes returns 422 (Pydantic validation error)
    - POST /api/v1/auth/login with valid credentials returns 200 with {"access_token": "...", "token_type": "bearer"}
    - POST /api/v1/auth/login with wrong password returns 401
    - GET /api/v1/users/me with valid Bearer token returns 200 UserResponse
    - GET /api/v1/users/me with no token returns 401
    - GET /api/v1/users/me with expired/invalid token returns 401
    - GET /health (at root, not /api/v1/health) returns 200 {"status": "ok", "version": "1.0.0"}
    - v1 health route at GET /api/v1/health also returns 200 (for future service-to-service use)
  </behavior>
  <action>
    Create backend/app/api/v1/auth.py:
    Import APIRouter, Depends, HTTPException, status from fastapi. Import AsyncSession. Import get_db from app.core.database. Import register_user, authenticate_user from app.services.user_service. Import create_access_token from app.core.security. Import UserCreate, UserResponse, UserLogin, TokenResponse from app.models.user.
    router = APIRouter(prefix="/auth", tags=["auth"]).
    POST /register: response_model=UserResponse, status_code=201. Body: user_create: UserCreate. Calls register_user(db, user_create). Returns UserResponse.model_validate(user).
    POST /login: response_model=TokenResponse. Body: user_login: UserLogin. Calls authenticate_user(db, user_login.email, user_login.password). If None: raise HTTPException(401, "Incorrect email or password"). Returns TokenResponse(access_token=create_access_token(user.id)).

    Create backend/app/api/v1/health.py:
    router = APIRouter(tags=["health"]).
    GET /health: no auth. Returns {"status": "ok", "version": "1.0.0"}.

    Create backend/app/api/v1/users.py:
    Import Annotated, Depends, APIRouter. Import get_current_user from app.api.deps. Import UserResponse from app.models.user. Import User from app.models.user.
    router = APIRouter(prefix="/users", tags=["users"]).
    GET /me: response_model=UserResponse. current_user: Annotated[User, Depends(get_current_user)]. Returns UserResponse.model_validate(current_user).

    Create backend/app/api/router.py:
    Import APIRouter. Import auth, health, users routers from v1 modules.
    router = APIRouter().
    router.include_router(auth.router) — no additional prefix (auth.router already has /auth prefix).
    router.include_router(health.router) — health has GET /health.
    router.include_router(users.router) — users has GET /users/me.

    Update backend/app/main.py (reads current state first — it has skeleton from Plan 02):
    Add: from app.api.router import router as api_router.
    After the app = FastAPI(...) line, add: app.include_router(api_router, prefix="/api/v1").
    Ensure the lifespan function includes: await seed_admin_if_absent() AND init_chroma(). Keep the existing root GET /health route (at /, not /api/v1/) for Docker healthcheck.
  </action>
  <verify>
    <automated>grep -n "include_router\|prefix.*api/v1\|seed_admin_if_absent\|asynccontextmanager" backend/app/main.py backend/app/api/router.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/api/v1/auth.py has POST /register with status_code=201 and response_model=UserResponse
    - backend/app/api/v1/auth.py POST /login returns TokenResponse with token_type="bearer"
    - backend/app/api/v1/users.py GET /me has Depends(get_current_user)
    - backend/app/api/router.py includes auth, health, and users routers
    - backend/app/main.py contains app.include_router(api_router, prefix="/api/v1")
    - backend/app/main.py lifespan calls seed_admin_if_absent() and init_chroma()
    - backend/app/main.py still has root GET /health route (not only /api/v1/health)
    - backend/app/main.py uses asynccontextmanager lifespan (not @app.on_event)
    - POST /login raises 401 (not 400 or 403) on invalid credentials
  </acceptance_criteria>
  <done>Complete auth flow: register, login, protected route. Admin seeded on startup. All routes versioned under /api/v1/ per D-03. Root /health for Docker healthcheck. v1/health for service-level health. System is end-to-end functional.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Internet → POST /register | Untrusted user input: email + password |
| Internet → POST /login | Untrusted credentials; timing attack surface |
| Internet → GET /users/me | JWT bearer token from Authorization header |
| FastAPI → PostgreSQL | SQLAlchemy parameterized queries for all user lookups |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-13 | Elevation of Privilege | POST /register — is_admin field | mitigate | is_admin not in UserCreate schema; service always sets is_admin=False; no user can self-promote |
| T-01-14 | Authentication bypass | POST /login credential timing | accept | bcrypt.checkpw() is constant-time; no additional mitigation needed for v1 |
| T-01-15 | Information Disclosure | POST /login error messages | mitigate | Single "Incorrect email or password" message regardless of whether email exists or password is wrong — no oracle for valid emails |
| T-01-16 | Tampering | JWT algorithm confusion | mitigate | decode_access_token uses algorithms=["HS256"] (list); alg:none rejected; enforced in security.py (Plan 02) |
| T-01-17 | Elevation of Privilege | Brute force on /login | accept | Rate limiting is v2 concern; not in scope for Phase 1 |
| T-01-18 | Spoofing | JWT token in query param | mitigate | OAuth2PasswordBearer extracts from Authorization: Bearer header only; query param tokens rejected |
</threat_model>

<verification>
Full end-to-end flow (can be tested manually or with pytest in Plan 05):
1. docker compose up --build
2. POST /api/v1/auth/register {"email": "test@example.com", "password": "testpass123"} → 201 {"id": 1, "email": "test@example.com", ...}
3. POST /api/v1/auth/login {"email": "test@example.com", "password": "testpass123"} → 200 {"access_token": "eyJ...", "token_type": "bearer"}
4. GET /api/v1/users/me with Authorization: Bearer eyJ... → 200 {"id": 1, ...}
5. GET /api/v1/users/me with no token → 401
6. GET /health → 200 {"status": "ok"}
</verification>

<success_criteria>
- FOUND-03: Register, login, and protected route all working end-to-end
- FOUND-04: GET /health returns 200 from FastAPI
- Admin seeded from env vars on first boot
- Duplicate email returns 409 (not 500)
- Invalid token returns 401 (not 403 or 500)
- All routes under /api/v1/ prefix per D-03
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-04-SUMMARY.md` when done
</output>
