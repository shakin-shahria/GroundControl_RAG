---
phase: 01-foundation
plan: 05
type: execute
wave: 5
depends_on:
  - 01-04
files_modified:
  - backend/tests/conftest.py
  - backend/tests/test_auth.py
  - backend/tests/test_health.py
  - backend/tests/test_db.py
  - backend/pyproject.toml
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04

must_haves:
  truths:
    - "pytest tests/ -x -q passes with all tests green"
    - "test_auth.py covers register (201), duplicate register (409), login (200 with token), login invalid (401), /users/me with token (200), /users/me without token (401)"
    - "test_health.py covers GET /health returning 200 with {status: ok}"
    - "test_db.py covers that all five tables exist in PostgreSQL after migration"
    - "pytest.ini asyncio_mode = auto is set so async tests run without explicit @pytest.mark.asyncio"
  artifacts:
    - path: "backend/tests/conftest.py"
      provides: "AsyncClient fixture, test DB session fixture"
      contains: "AsyncClient"
    - path: "backend/tests/test_auth.py"
      provides: "Auth flow integration tests"
      contains: "test_register_success"
    - path: "backend/tests/test_health.py"
      provides: "Health endpoint test"
      contains: "test_health_endpoint"
    - path: "backend/tests/test_db.py"
      provides: "Schema existence tests"
      contains: "test_tables_exist"
    - path: "backend/pyproject.toml"
      provides: "pytest config with asyncio_mode = auto"
      contains: "asyncio_mode"
  key_links:
    - from: "backend/tests/conftest.py"
      to: "backend/app/main.py"
      via: "AsyncClient(app=app, base_url=...)"
      pattern: "AsyncClient.*app"
    - from: "backend/tests/test_auth.py"
      to: "POST /api/v1/auth/register"
      via: "client.post('/api/v1/auth/register', ...)"
      pattern: "api/v1/auth/register"
---

<objective>
Write the complete pytest integration test suite covering all four FOUND requirements. Tests run against a test PostgreSQL database (configured via environment). Provides the automated gate for phase verification.

Purpose: Every FOUND-0X requirement must have an automated test that proves it works. The test suite is the verification artifact for this phase.
Output: conftest.py (AsyncClient fixture), test_auth.py (FOUND-03), test_health.py (FOUND-04), test_db.py (FOUND-01), pyproject.toml (pytest config). Docker Compose smoke test for FOUND-02 is documented as a manual step.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-RESEARCH.md
@.planning/phases/01-foundation/01-04-SUMMARY.md
@CLAUDE.md

<interfaces>
<!-- API contracts established in Plan 04 that tests will call -->

POST /api/v1/auth/register
  Request: {"email": "str", "password": "str"}
  Response 201: {"id": int, "email": "str", "is_admin": bool, "created_at": "datetime str"}
  Response 409: {"detail": "Email already registered"}
  Response 422: Pydantic validation error (e.g. password > 72 bytes)

POST /api/v1/auth/login
  Request: {"email": "str", "password": "str"}
  Response 200: {"access_token": "str", "token_type": "bearer"}
  Response 401: {"detail": "Incorrect email or password"}

GET /api/v1/users/me
  Request header: Authorization: Bearer <token>
  Response 200: {"id": int, "email": "str", "is_admin": bool, "created_at": "str"}
  Response 401: no/invalid/expired token

GET /health
  Response 200: {"status": "ok", "version": "1.0.0"}

Database tables (after alembic upgrade head):
  users, documents, document_chunks, ai_requests, evaluations
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create pyproject.toml and tests/conftest.py</name>
  <files>
    backend/pyproject.toml,
    backend/tests/conftest.py
  </files>
  <read_first>
    - .planning/phases/01-foundation/01-RESEARCH.md (Validation Architecture section; Wave 0 Gaps; test framework: pytest-asyncio asyncio_mode=auto)
    - backend/app/main.py (app object to import for AsyncClient)
    - backend/app/core/database.py (engine, Base, get_db — needed for test DB override)
    - backend/requirements.txt (confirm pytest==9.1.0, pytest-asyncio==1.4.0, httpx==0.28.1 are present)
  </read_first>
  <action>
    Create backend/pyproject.toml:
    [tool.pytest.ini_options] section: asyncio_mode = "auto". testpaths = ["tests"]. This eliminates the need for @pytest.mark.asyncio on every async test function.

    Create backend/tests/conftest.py:
    Import: pytest, AsyncClient from httpx, AsyncGenerator from typing. Import app from app.main. Import engine, Base, async_session_maker, get_db from app.core.database. Import AsyncSession from sqlalchemy.ext.asyncio.

    Strategy: Tests use the same app with the real database (test database configured via TEST_DATABASE_URL env var, or falls back to DATABASE_URL). For CI simplicity, tests run against a test-specific PostgreSQL DB (the test env creates and drops tables). For now, tests assume the application DB is accessible and migration has already run (same as docker compose does).

    Define fixture client() as async with AsyncClient(app=app, base_url="http://test") as ac: yield ac. Scope: function.

    Define fixture registered_user(client) that calls POST /api/v1/auth/register with a test email/password and yields the response JSON. Use a unique email per test to avoid conflicts: use uuid4 to generate email like f"test-{uuid4()}@example.com".

    Define fixture auth_token(client, registered_user) that calls POST /api/v1/auth/login with the registered user's credentials and yields the access_token string.

    Define fixture auth_headers(auth_token) that yields {"Authorization": f"Bearer {auth_token}"}.
  </action>
  <verify>
    <automated>grep -n "asyncio_mode\|AsyncClient\|registered_user\|auth_headers" backend/pyproject.toml backend/tests/conftest.py</automated>
  </verify>
  <acceptance_criteria>
    - backend/pyproject.toml contains asyncio_mode = "auto" under [tool.pytest.ini_options]
    - backend/tests/conftest.py contains "AsyncClient(app=app" (httpx async client wrapping the FastAPI app)
    - conftest.py defines client, registered_user, auth_token, and auth_headers fixtures
    - registered_user fixture uses a unique email per call (uuid4 or similar) — no hardcoded email that would cause 409 on second run
  </acceptance_criteria>
  <done>pytest configured with asyncio auto mode. AsyncClient fixture wraps the FastAPI app directly (no real HTTP server needed). Fixtures composable: auth_headers depends on auth_token depends on registered_user depends on client.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create test_auth.py, test_health.py, and test_db.py</name>
  <files>
    backend/tests/test_auth.py,
    backend/tests/test_health.py,
    backend/tests/test_db.py
  </files>
  <read_first>
    - backend/tests/conftest.py (fixture names and what they yield)
    - .planning/REQUIREMENTS.md (FOUND-01, FOUND-03, FOUND-04 exact pass/fail criteria)
    - .planning/ROADMAP.md (Phase 1 success criteria list — use as test case source)
    - backend/app/core/database.py (engine — needed for table introspection in test_db.py)
  </read_first>
  <behavior>
    test_auth.py:
    - test_register_success: POST /register with valid data → 201, response has id, email, is_admin=False
    - test_register_duplicate_email: POST /register twice with same email → second returns 409
    - test_register_password_too_long: POST /register with 80-char password → 422
    - test_login_success: POST /login with valid credentials → 200, response has access_token and token_type="bearer"
    - test_login_wrong_password: POST /login with wrong password → 401
    - test_login_unknown_email: POST /login with unregistered email → 401
    - test_get_me_authenticated: GET /users/me with valid token → 200, response email matches registered user
    - test_get_me_no_token: GET /users/me with no Authorization header → 401
    - test_get_me_invalid_token: GET /users/me with "Authorization: Bearer invalid-token" → 401

    test_health.py:
    - test_health_endpoint: GET /health → 200, body has status="ok"

    test_db.py:
    - test_tables_exist: After migration, all five tables (users, documents, document_chunks, ai_requests, evaluations) exist in PostgreSQL. Use SQLAlchemy text("SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'") via an async DB session to fetch table names, then assert each expected table name is present.
  </behavior>
  <action>
    Create backend/tests/test_auth.py:
    All test functions are async. Use the client, registered_user, auth_headers fixtures from conftest.
    For test_register_success: send POST /api/v1/auth/register with {"email": unique_email, "password": "TestPass123"}, assert status 201, assert response JSON has "id" key, assert "is_admin" is False.
    For test_register_duplicate_email: register same email twice, assert second response status is 409.
    For test_register_password_too_long: send password as "a" * 80 (80 ASCII chars = 80 bytes > 72), assert status is 422.
    For test_login_success: after registering, POST /api/v1/auth/login, assert 200, assert "access_token" in response, assert token_type == "bearer".
    For test_login_wrong_password: POST /login with wrong password, assert 401.
    For test_login_unknown_email: POST /login with email that was never registered, assert 401.
    For test_get_me_authenticated: GET /api/v1/users/me with auth_headers fixture, assert 200, assert response JSON "email" matches registered user email.
    For test_get_me_no_token: GET /api/v1/users/me with no headers, assert 401.
    For test_get_me_invalid_token: GET /api/v1/users/me with {"Authorization": "Bearer not-a-real-token"}, assert 401.

    Create backend/tests/test_health.py:
    test_health_endpoint: GET /health (no auth), assert status == 200, assert response.json()["status"] == "ok".

    Create backend/tests/test_db.py:
    Import text from sqlalchemy, AsyncSession from sqlalchemy.ext.asyncio, pytest. Import async_session_maker from app.core.database.
    test_tables_exist: async function (no fixture needed — uses async_session_maker directly). Opens a session, executes SELECT table_name FROM information_schema.tables WHERE table_schema = 'public'. Fetches all rows. Asserts each of ["users", "documents", "document_chunks", "ai_requests", "evaluations"] is in the returned table names set.
  </action>
  <verify>
    <automated>grep -n "def test_" backend/tests/test_auth.py backend/tests/test_health.py backend/tests/test_db.py | wc -l</automated>
  </verify>
  <acceptance_criteria>
    - backend/tests/test_auth.py contains at least 9 test_ functions (register_success, duplicate, too_long, login_success, wrong_password, unknown_email, me_auth, me_no_token, me_invalid)
    - backend/tests/test_health.py contains test_health_endpoint that asserts status == 200 and body has status="ok"
    - backend/tests/test_db.py contains test_tables_exist that checks all five table names
    - test_register_password_too_long uses a password that is > 72 bytes (80 ASCII chars is 80 bytes, which is correct)
    - test_get_me_authenticated asserts response email matches the registered user's email (not just that status is 200)
    - All test functions are async (async def test_...)
  </acceptance_criteria>
  <done>All FOUND requirements have automated test coverage. pytest tests/ -x -q runs the full suite. test_db.py proves FOUND-01 (tables exist). test_health.py proves FOUND-04. test_auth.py proves FOUND-03. FOUND-02 is verified via docker compose up smoke check (manual step documented in success criteria).</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process → FastAPI app | In-process via httpx AsyncClient; no real HTTP; no network exposure |
| Test process → PostgreSQL | Direct DB connection via async_session_maker for schema introspection |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-19 | Information Disclosure | Test credentials in test files | accept | Test credentials are not real; test files are never deployed; hardcoded test data is acceptable in test layer |
| T-01-20 | Tampering | Test database isolation | mitigate | registered_user fixture uses uuid4-based unique email per test — no test data leaks between test functions |
</threat_model>

<verification>
Run from backend/ directory (with .env present and DB accessible):

```
pytest tests/ -x -q
```

Expected output: all tests pass, 0 errors. Individual suite verification:
- pytest tests/test_health.py -v → test_health_endpoint PASSED
- pytest tests/test_auth.py -v → all 9 auth tests PASSED
- pytest tests/test_db.py -v → test_tables_exist PASSED (requires running DB with migration applied)

Docker Compose smoke (FOUND-02 — manual):
```
docker compose up -d --build
docker compose ps
```
All three services show status "healthy". No restart loops. Backend logs show "alembic upgrade head" completing before uvicorn starts.
</verification>

<success_criteria>
- pytest tests/ -x -q exits with code 0 (all tests green)
- test_auth.py covers all 9 auth behaviors listed in acceptance criteria
- test_db.py confirms all five tables exist
- test_health.py confirms GET /health returns 200
- pyproject.toml has asyncio_mode = "auto" (no manual marks needed)
- FOUND-02 (Docker Compose with health checks) verified manually via docker compose up
</success_criteria>

<output>
Create `.planning/phases/01-foundation/01-05-SUMMARY.md` when done
</output>
